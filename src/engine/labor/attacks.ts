import { getLabor } from "./content.js";import type{Requirement}from"../requirements/evaluate.js";
export interface AttackDefinition{scope:string;requirement:Requirement;damage:number;multipleDisjoint?:boolean;}
export function attackForLaborDie(laborId:string,laborDieId:string):AttackDefinition{const labor=getLabor(laborId);const dice=(labor.labor_dice as Array<Record<string,unknown>>|undefined)??[];const die=dice.find(d=>d.id===laborDieId);if(!die)throw new Error(`Unknown Labor die ${laborDieId}.`);if(labor.attack){const attack=labor.attack as Record<string,unknown>;return{scope:attack.scope as string,requirement:attack.requirement as Requirement,damage:(attack.damage as number|undefined)??1,multipleDisjoint:attack.multiple_disjoint_attacks_per_roll===true};}const attackId=(die.attack_id??die.attack_group) as string|undefined;const attacks=labor.attacks as Record<string,Requirement>|undefined;if(!attackId||!attacks?.[attackId])throw new Error(`Labor die ${laborDieId} has no attack definition.`);return{scope:"target",requirement:attacks[attackId],damage:1};}
