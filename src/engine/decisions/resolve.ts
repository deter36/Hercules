import type{GameState}from"../state/types.js";import{breakDie}from"../dice/lifecycle.js";import{applyTemporaryDiceDelta}from"../dice/pool.js";import{enforceHindSoftLock}from"../labor/hind.js";import{cleanupRound}from"../round/resolve.js";
export function chooseBrokenDie(state:GameState,decisionId:string,dieId:string):GameState{const pending=state.pendingDecision;if(!pending||pending.id!==decisionId||pending.type!=="CHOOSE_DIE_TO_BREAK")throw new Error("No matching broken-die decision.");if(!pending.legalOptions.some(o=>o.id===dieId))throw new Error("Illegal die choice.");const next=structuredClone(state);next.herculesDice[dieId]=breakDie(next.herculesDice[dieId]);next.pendingDecision=null;const checked=enforceHindSoftLock(next);if(checked.game.phase==="DEFEAT")return checked;checked.game.phase="FAILURE_CHECK";return cleanupRound(checked);}
export function chooseBranch(state:GameState,decisionId:string,nodeId:string):GameState{const pending=state.pendingDecision;if(!pending||pending.id!==decisionId||pending.type!=="CHOOSE_TRACK_BRANCH")throw new Error("No matching branch decision.");if(!pending.legalOptions.some(o=>o.id===nodeId))throw new Error("Illegal branch choice.");const next=structuredClone(state);const laborDieId=pending.context.laborDieId;if(typeof laborDieId==="string"&&next.currentLabor?.laborDice[laborDieId]){next.currentLabor.laborDice[laborDieId].nodeId=nodeId;next.game.phase="IMPACT_RESOLUTION";}next.pendingDecision=null;return next;}
export function chooseGhostAbderusCost(state:GameState,decisionId:string,optionId:"lose_die"|"lose_spirit"):GameState{const pending=state.pendingDecision;if(!pending||pending.id!==decisionId||pending.type!=="CHOOSE_GHOST_ABDERUS_COST")throw new Error("No matching Ghost of Abderus decision.");if(!pending.legalOptions.some(o=>o.id===optionId))throw new Error("Illegal Ghost of Abderus choice.");let next=structuredClone(state);if(optionId==="lose_die")next=applyTemporaryDiceDelta(next,-1);else if(typeof next.player.spirit==="number")next.player.spirit=next.player.spirit<=5?"SKULL":next.player.spirit-5;next.pendingDecision=null;if(next.player.spirit==="SKULL"){next.game.phase="DEFEAT";next.game.result="defeat";}else next.game.phase="READY_TO_ROLL";return next;}
export function choosePholusReward(state:GameState,decisionId:string,rewardId:string):GameState{const pending=state.pendingDecision;if(!pending||pending.id!==decisionId||pending.type!=="CHOOSE_PHOLUS_REWARD")throw new Error("No matching Ghost of Pholus decision.");if(!pending.legalOptions.some(o=>o.id===rewardId))throw new Error("Illegal Ghost of Pholus Reward choice.");const next=structuredClone(state);next.player.temporaryEffects.push({type:"disabled_reward",rewardId,duration:"labor",source:"mood.ghost_pholus"});next.pendingDecision=null;next.game.phase="READY_TO_ROLL";return next;}
