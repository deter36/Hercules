import type{HerculesDieState}from"../state/types.js";
const opposite=(n:number)=>7-n;const wrap=(n:number)=>((n-1+6)%6)+1;
export function applyDieAbility(die:HerculesDieState,ability:Record<string,unknown>,target?:number):HerculesDieState{if(die.face===null)throw new Error("Ability requires a rolled die.");let face=die.face;switch(ability.type){case"modify_pip":case"increment_pip":face=(ability.wrap===true?wrap(face+(target??(ability.type==="increment_pip"?1:0))):Math.max(1,Math.min(6,face+(target??0))));break;case"flip_opposite_side":face=opposite(face);break;case"odd_even_toggle":face=face%2?face+1:face-1;break;case"set_any":if(!target||target<1||target>6)throw new Error("Ability requires a valid target face.");face=target;break;case"map_value":{const mapping=ability.mapping as Record<string,number>;const mapped=mapping?.[String(face)];if(!mapped)throw new Error(`No verified mapping for face ${face}.`);face=mapped;break;}default:throw new Error(`Unsupported die ability ${String(ability.type)}.`);}return{...die,face,blueUsed:true};}
