import assert from "node:assert/strict"; import test from "node:test"; import { applySimultaneous } from "../../src/engine/resources/resolve.js"; import { createInitialState } from "../../src/engine/state/create.js"; import { startLabor, resolveMood } from "../../src/engine/labor/setup.js"; import { commitInitialRoll, resolveRoundDamage } from "../../src/engine/round/resolve.js"; import { placeGoldAbility } from "../../src/engine/actions/placement.js";
test("resources net simultaneous effects before cap",()=>{assert.equal(applySimultaneous(15,[3,-2],17,"SKULL"),16);assert.equal(applySimultaneous(17,[3],17,"SKULL"),17);assert.equal(applySimultaneous(17,[-1],17,"SKULL"),16);assert.equal(applySimultaneous(9,[2,-1],10,"TOP"),10);assert.equal(applySimultaneous(2,[-3],17,"SKULL"),"SKULL");});
test("gold gains and entered-node losses net before Spirit bounds",()=>{let state=resolveMood(startLabor(createInitialState("human","simultaneous-gold-impact"),"labor.L01"),"mood.ferocious");state.player.spirit=16;state.player.ownedRewardIds.push("reward.L04.A");state.currentLabor!.laborDice["labor.L01.d1"].nodeId="L01.n3";state=commitInitialRoll(state,{H1:4,H2:4,H3:2,H4:3,H5:5});state.game.phase="GOLD_AND_ATTACK_PLACEMENT";state=placeGoldAbility(state,"ability.reward.L04.A.gold",["H1","H2"]);assert.equal(state.player.spirit,16);assert.deepEqual(state.round.resourceQueue.spiritDeltas,[2]);state=resolveRoundDamage(state);assert.equal(state.player.spirit,16);assert.deepEqual(state.round.resourceQueue.spiritDeltas,[]);});
